{
  "compilerOptions": {
    "target": "esnext",
    "lib": ["dom", "dom.iterable", "esnext"],
    "noEmit": true,

    // If there's any 3rd-party conflict, skip it
    "skipLibCheck": true,

    // Next.js recommended
    "strict": false,
    "module": "esnext",
    "moduleResolution": "node",
    "jsx": "preserve",
    "resolveJsonModule": true,
    "allowJs": true,
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "isolatedModules": true,

    // Type definitions
    "types": ["next", "node"],

    // Example path aliases
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },

    // Next.js plugin
    "plugins": [
      {
        "name": "next"
      }
    ]
  },
  "include": ["src", ".next/types/**/*.ts", "next-env.d.ts"],
  "exclude": ["node_modules"]
}
